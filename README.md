# kuma-versionizer
Automatically fetches each service’s reported version, turns it into a tag, and keeps the matching Uptime Kuma monitor updated—so your dashboard always shows the live version without manual tweaks.
